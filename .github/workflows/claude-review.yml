name: Claude Code Review

# Placeholder workflow for Video 7 — Claude-powered PR reviews.
# This will be fully implemented in a future session.

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize]

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Placeholder
        run: echo "Claude code review will be configured in Video 7"
